# Local tooling / session state — not project source
.claude/
.remember/

# Python
__pycache__/
*.pyc
.venv/

# Personal config overrides
config/user.yaml
.env

# User data — private debate transcripts (NFR-S-01)
data/

# Large model files — fetched by scripts/fetch_models.py
models/
*.gguf
*.onnx

# Audio fixtures — except the ones the test suite depends on
tests/fixtures/audio/*.wav
!tests/fixtures/audio/silence.wav
!tests/fixtures/audio/README.md

# OS cruft
Thumbs.db
.DS_Store
