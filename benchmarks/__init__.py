"""Standalone benchmark harness support.

The benchmark package must not import application code from ``src/``. It exists
only to make measurement logic testable and repeatable before feature work.
"""
